# Domain Docs

This is a single-context repository.

Before substantial implementation work, read `docs/spec.md` for the current product behavior, provider language, account model, runtime expectations, and UI contracts.

Architectural decision records should live under `docs/adr/` when they exist. If ADRs are added later, skills should read relevant ADRs before changing behavior in that area.
